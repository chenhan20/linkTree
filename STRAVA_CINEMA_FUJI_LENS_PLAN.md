# Strava Cinema：Fuji Lens Mode 視覺精修任務

## 給 Fable 5.1 的直接指令

請在**現有完成版**上做一次集中式視覺精修，不要重寫網站，也不要改變既有功能與資料結構。

- 唯一主體：`/Users/stevechuangmbp2024/Desktop/github/linkTree/strava_cinema.html`
- 可配合修改：
  - `/Users/stevechuangmbp2024/Desktop/github/linkTree/theme-strava-cinema.css`
  - `/Users/stevechuangmbp2024/Desktop/github/linkTree/strava-cinema-fx.js`
- 視覺示意圖：`/Users/stevechuangmbp2024/Desktop/github/linkTree/docs/review/strava-cinema-fuji-lens-concept.png`
- **禁止開啟、分析或參考 `strava_fable51.html`。**
- 不要另建一套設計，不要推翻現有 `Taiwan Ride Cinema`，不要把版面改回一般 dashboard。
- 所有既有功能、資料、九個 view、舊 hash、深連結、drawer、modal、圖表、3D、Atlas、鍵盤與返回行為都要保留。

示意圖只用來傳達「鏡頭介面、構圖、控制密度與電影色彩」。請沿用網站現在真正的影片、照片、車手與資料，不要照抄示意圖中 AI 生成的人臉或文字。

## 想要的核心感覺

把目前已經很滿意的台灣騎行背景，再往「正在透過一台富士相機觀看自己的騎行電影」推進一步：有光學對焦、光圈、曝光與底片色彩的感覺，但網頁本身仍然是一個清楚、成熟、可操作的訓練介面。

這不是把整台相機畫在網站上，也不是把網站做成相機 App。只借用觀景窗最有魅力的幾個訊號：對焦、呼吸感、底片色彩與一次性的緩慢推鏡。

## 1. TODAY 開場：半按快門到鎖定主角

每次進入 TODAY，媒體層只演一次：

1. `0–0.5 秒`：畫面稍暗、輕微失焦，顯示很淡的曝光資訊。
2. `0.5–1.2 秒`：模擬光圈張開，亮度與景深自然變化。
3. `1.2–1.7 秒`：細小綠色 AF 框在車手頭部附近搜尋，鎖定後短暫亮一下。
4. `1.7–8 秒`：開始非常緩慢的 tracking push-in，由 `scale(1)` 推至目前 720p 素材安全上限 `scale(1.05)`。
5. `8 秒後`：停止推進並停在最終構圖；絕對不要縮回去，也不要循環呼吸縮放。

最終構圖以車手「安全帽到膝部」為主，手把完整保留，前輪下緣可以裁掉；車手維持在畫面右側約 `63–68%`，左側保留山勢、道路與 TODAY 資料空間。

只允許背景照片／影片／Canvas 媒體層移動，標題、數據、卡片、按鈕與導覽列必須完全固定且始終清晰。

### 對焦效果規則

- 使用光學失焦感：柔和 blur、bloom、輕微曝光呼吸與邊緣色散。
- **不要使用馬賽克、像素格、碎片瓦片或全頁轉場。**
- 對焦演出只用於初次進入 TODAY；切換 tab/view 時採短 crossfade 或原有轉場，不可每次重新失焦。
- `prefers-reduced-motion` 或 QUIET 模式下，直接顯示已鎖定的靜態最終構圖。

## 2. 光圈與景深控制

在既有 `FX · OVERDRIVE` 面板增加一列 `LENS`：

- `F2.0`：最電影化，背景柔、光點較大、車手最突出。
- `F2.8`：預設值，保留道路環境，主角仍有明顯層次。
- `F5.6`：資料閱讀模式，環境清楚、效果克制。
- `F8`：近乎全清楚，適合訓練、趨勢與大量圖表頁。

不要單純把整張畫面加同一層 blur。請沿用現有景深分層，把前景、主體附近與遠景分開調整；UI 層永遠不進濾鏡。

建議自動對應：

| View | 預設光圈 |
| --- | --- |
| TODAY | F2.8 |
| RIDE | F2.8 |
| TRAIN | F5.6 |
| REVIEW | F5.6 |
| ALL | F8 |

使用者手動選擇後，以 `localStorage` 保留選項；另提供 `AUTO` 讓系統恢復依 view 自動判斷。

## 3. 經典底片色彩選擇

在同一個 FX 面板加入 `FILM SIMULATION`：

- `AUTO`：依 view 與狀態自動選擇。
- `STD`：清楚、中性、資料頁安全，概念接近標準正片。
- `CC`：低飽和、暖高光、偏紀實，適合台灣街道與騎行記錄。
- `ETERNA`：柔和高光、低對比電影色，作為 TODAY 預設。
- `CN`：較有個性、微冷陰影與暖色主體，適合城市與夜景。
- `ACROS`：黑白、高階灰階，只在使用者主動選擇時套用。

第一版不要加入高飽和模式，避免黃色車衣、綠色植物與橘色 UI 一起爆色。這些是「受經典底片觀感啟發」的網站調色，不要求複製任何相機廠的精確色彩科學。

濾鏡只能作用在照片、影片與環境 Canvas。不得降低中文、數字、圖表、按鈕或提示文字的可讀性。

切換濾鏡時可做一次約 `250ms` 的微型光圈閉合 → 換色 → 張開，但不可黑屏、不可馬賽克，也不可重新播放完整開場。

## 4. 觀景窗 HUD：有味道，但退到第二層

TODAY 可加入以下淡化元素：

- 車手安全帽／臉部附近的一個細綠色 AF 框。
- 左上小型 `AF-C`。
- 右上小型 `REC` 或狀態點。
- 底部非常細的曝光資訊：`F2.8`、`1/250`、`ISO 400` 與曝光刻度。

HUD 在鎖定後降低到約 `25–35%` 視覺強度，8 秒後再淡一些；滑鼠停在媒體區或打開 FX 面板時才稍微恢復。不要覆蓋 TODAY 的資料，不要把 HUD 做成比訓練狀態更醒目的資訊。

## 5. FX 面板樣式

沿用右上方現在的 `FX · OVERDRIVE`，打開後成為小而精緻的半透明黑色控制面板：

- 第一列：`FILM SIMULATION`。
- 第二列：`LENS`。
- 目前選項使用暖琥珀色描邊；對焦成功使用少量富士式綠色，只當功能訊號。
- 桌機不可超過約 `360px` 寬。
- iPhone 改成底部 sheet，按鈕至少 `44px` 高。
- 點外側、Esc、關閉鈕皆可收起，並維持目前明顯的返回行為。

不要常駐一個巨大的光圈轉盤，也不要增加多層設定頁。這是快速調味控制，不是相機選單模擬器。

## 6. 狀態與未來影片的接口

目前先繼續使用現有影片，不要為等待新素材而停工。請把媒體來源整理成可擴充映射，保留之後依四種 `todayVerdict()` 狀態換片的接口：

- `go`
- `ease`
- `rest`
- `nodata`

現階段未提供的狀態影片全部安全 fallback 到目前影片／poster；不可出現空白 hero，也不要隨機播放天氣。未來新影片到位後，只需補檔案路徑，不應再改 UI 邏輯。

### 騎行電影場景輪播

媒體接口同時保留三個場景：`mountain`、`river`、`city`。這三個場景不是隨機背景，而是一卷連續的騎行短片：山線代表意志與爬升、河濱代表呼吸與節奏、城市代表速度與台北濕夜。

- 預設在每支影片自然播完後依序前進：`MOUNTAIN → RIVER → CITY`。
- 第一次開啟 TODAY 才播放對焦鎖定；後續換片不再 blur 或重新對焦。
- 禁止 crossfade 疊出兩個車手。換片使用 `220–300ms` 的短 shutter blink／暖色漏光，迅速壓暗後直接開下一片。
- 提供低調的 `山線／河濱／城市` 手動切換；自動輪播仍預設開啟。
- modal、drawer、頁面失焦或媒體 hover 時暫停輪播。
- 只載入目前與下一支影片；不要同時解碼全部影片。
- reduced motion、QUIET 或行動裝置省電情境，使用相應 poster，不強制播放。

## 7. 實作順序

1. 先完成 TODAY 的一次性對焦與 push-in，確定最終構圖正確。
2. 再加入 LENS 光圈選擇與景深對應。
3. 再加入 FILM SIMULATION 與 AUTO 對應。
4. 最後整理 FX 面板、iPhone bottom sheet、reduced motion 與媒體 fallback。

請自行連續完成，不要每一小步停下來詢問；但也不要順手重構和本任務無關的資料、圖表或九個 view。

## 8. 驗收條件與測試節制

先交一個可在瀏覽器直接看的完整版本，讓我確認視覺方向；**在我確認前，不要再次跑 67 項完整回歸，也不要遍歷大量解析度。**

視覺確認後，只需測：

- 桌機：`1440 × 900` 左右。
- iPhone：`390 × 844`。
- console 無例外。
- TODAY 開場只播放一次，切 view 不重播。
- QUIET／reduced motion 正常。
- UI 文字沒有被 blur/filter。
- 所有既有功能、hash 與深連結仍可用。

## 明確不要做

- 不要開啟或研究 `strava_fable51.html`。
- 不要重寫或另做 `strava_cinema_v2.html`。
- 不要改掉目前滿意的台灣騎行背景與中文排版。
- 不要做整頁馬賽克轉場。
- 不要把實體相機機身畫進網站。
- 不要持續循環 zoom、抖動、手持晃動或反向 zoom out。
- 不要讓濾鏡套到 UI、文字與數據。
- 不要先花大量時間做全面自動化測試。

完成時請直接說明：修改了哪些檔案、如何切換濾鏡／光圈、未來狀態影片要放在哪個映射，以及有哪些地方需要我用肉眼確認。
