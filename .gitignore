athlete/*.csv
scripts/.env
__pycache__/

# FIT 同步的執行日誌：append-only，每班整份重寫再 commit 一次。
# 44K × 每天兩班 ≈ 一年 32MB 的 git 垃圾，跟一年份 FIT 本體差不多大，
# 而且 Actions 的執行紀錄本來就看得到。留在工作目錄，不要進版控。
data/fit/_sync.log

# Strava 封存的「原始活動明細」：140 趟 × 完整 segment_efforts / laps / splits / polyline，
# 是 harvest-strava.js 的中間產物。真正不可取代的東西已經萃取到會進版控的三個檔案裡：
#   data/strava-archive/index.json            活動清單
#   data/strava-archive/segments/*.json       路段折線與 metadata ← 訂閱到期後就拿不到的
#   data/strava-archive/segment-catalog.json  每一條路段的每一筆成績（聚合後）
# 這包原始檔隨時可以用 --phase details 重抓（額度夠），不值得讓 repo 多幾十 MB。
# 想留就自己備份到 repo 外面。
data/strava-archive/activities/

# 封存的路段原始折線（77 條、9.2 MB、每條 800~2000 點）。
# 刻意不進版控：那是別人建立的「路段資料」，不是自己的活動資料，
# 而這個 repo 是公開的（Strava API Agreement 對第三方快取有 7 天限制）。
# 真的會用到的那幾條，降採樣成 140 點之後存在 data/segment-streams.json 裡，
# 那份本來就公開、也只放已設成 ITT 的路段。
# 要把某條接上自建計時：加進 data/itt-config.json → node scripts/merge-harvested-streams.js
data/strava-archive/segments/
