# 2026-08-13 工作紀錄與交接

> 給下一次接手的人（或未來的自己）。目的是**不用重讀對話就能接下去**。
> 相關文件：`fit-pipeline.md`（FIT 管線全貌）、`intervals-api-survey.md`（API 盤點）。

---

## 一、這天完成並已 push 的

| commit | 內容 |
|---|---|
| `08e39fd` | fit-sync 排程改每天兩班（台灣 10:30 / 22:30） |
| `153ce50` | gitignore `__pycache__/` |
| `5baf639` | **產報告門檻改功率優先 + 同日去重**（問題 A） |
| `d3e71ae` | **自建 ITT 路段計時偵測器** `tools/tcx/segments.py` |
| `17434d6` | intervals.icu API 盤點文件 |
| `13a38d0` | 名言資料源搬到獨立 repo steve-quotes |
| `e7606a3` | 課表檔名改「好兄弟月」 |
| `7bea19e` | **標題來源脫離 Strava**，改用 intervals.icu 活動名稱 |
| `fb76166` | **壞掉的 FIT 不再每班重炸；失敗不會丟掉當班成果** |
| `92faf42` | 新增三條河濱 ITT 路段 + `scan_segments` 開關 |
| `d411fec` | ITT 檯面改成算出來的固定版面，移除拖曳 |
| `4ee7ac0` | 三條新路段的成績／軌跡／地形（本機掃描補齊） |

### 幾個不重讀對話就想不起來的重點

**FIT 有 commit 進 repo，而且應該繼續 commit。** 一年約 45 MB，GitHub 撐十幾年沒問題。
關鍵是**刪掉救不了**：git 歷史永久保留每個 commit 過的檔案，事後刪只是從工作目錄消失。
而且 FIT 現在是主要資料源（ITT 偵測器直接讀它、報告從它重生），不是快取。

**排程實際延遲 13～56 分鐘。** strava-sync 與 fit-sync 當初錯開 30 分的設計已經失效，
兩支會撞車，所以都加了 `git push || (git pull --rebase && git push)`。

**GitHub Actions 全自動、零 LLM 成本。** 整條是純 Python，沒有任何模型參與。

---

## 二、未 commit 的：課表對帳評分（**本次重點**）

檔案：`data/plan.json`（新）、`tools/tcx/score.py`（新，1234 行）、
`scripts/build-ride-reports.py`、`tools/tcx/render_dashboard.py`、`tools/tcx/build_index.py`、
`activity-modal.js` / `activity-modal.css`（活動彈窗的徽章）。

### 為什麼做

用戶明確抱怨：8/13 的報告寫得像「你在爽騎」，但他是照課表執行的。
根因在 `analyze_tcx.py` 的 `training_quality()`：把任何 IF < 0.75 的自動分段算成
「移動但沒在練」。**這是爬坡指標** —— 爬坡低功率＝滑行＝真沒練；
平路課表的低功率是**處方寫死的熱身與恢復**。8/13 的處方光是熱身 20 分 +
緩衝 8 分 + 收操 12 分就有 40 分鐘照規定該低於門檻，於是 `effective_pct` 只有 44.9%。

**設計原則：有處方就對帳處方，不要用通用啟發式判斷「有沒有在練」。**
沒有處方的自由騎才退回原本的邏輯。

### 8/13 實跑結果（已驗證，數字可信）

```
總分 85.5 / 100   等第 A-
  執行度 95.0 x0.40   紀律 77.4 x0.25   續航 100.0 x0.20   迴轉 54.3 x0.15

主測 1  處方 60分 >=185W  ->  實際 60.0分 平均 185W   100 分
        前半 173W / 後半 197W (+13.9%)   心率 154/169
主測 2  處方 20分 全力     ->  實際 15.2分 平均 212W   80 分（只做 76% 時間）

校準產出① 60分@185W：撐得住 -> 四週表數字照跑
校準產出② 疲勞後 20 分功率 208W（15.2 分實測 212W 外推）
          對照 2025-09 新鮮 252W = -17.3%
          對照 2026-04 新鮮 240W = -13.2%   <- 斷崖的實際大小
規則違規  主測 1 低於 165W 超過 10 秒：16 次、271 秒（7.5%）
心率      主測 1 有 65% 時間 >150bpm，沒降瓦（照課表，僅記錄不扣分）
```

跟舊指標的 44.9% 是完全相反的結論，而且每個數字都可查。

### 對抗複驗結果（已跑完）

**通過的**：對齊沒有灌水（DP 完全沒讀裝置 lap，但算出來跟手動 lap 差 6 秒；
難看的前半 173W 出現在四個地方沒被藏）、分數真的會變差（同一個檔硬套不同處方
得 66.5 / 67.8 / 75.1 / 79.0，8/11 套 9/01 只有 59.8）、沒有處方的日子不打分、
既有數字沒被弄壞、徽章渲染正常、工作區乾淨。

**已修（本次）**：
- 跑步 FIT 會被騎車課表打分（Garmin 跑步功率通過了 have_power 檢查）→ 加 sport 檢查
- 作用段一段都沒對到時，紀律給 100 分並印出「全程沒有一次低於 165W」這種**假話**
  → 改成 `score:None` 不計分、verdict 寫「無從判定」

**還沒修，依嚴重度**（每條都有可復現指令）：

| # | 嚴重度 | 問題 |
|---|---|---|
| 1 | **P0** | **執行度只看段平均，不看離散度。** 復現：`score.py data/fit/2026-08-11_*.fit --date 2026-09-08` → 爬坡日（VI 1.25）套減量日「Z2 70分 @160-175W」拿 **100 分**，實際只有 7.5% 的秒數落在區間內、18.2% 在 20W 以下滑行。**這正是課表第 2 節「坡會替你踩」要修的問題，評分器卻獎勵它。** 修法：執行度要納入「落在區間內的秒數佔比」，不能只看平均。 |
| 2 | **P0** | **allout 段功率永遠 100 分**（`score.py:508 if tw.get('allout'): return 100.0`），地板只有 FTP×0.6＝142.8W。9/10 驗收日的主測 2 也是 allout，同一個洞。修法：跟該時長的個人最佳比。 |
| 3 | P1 | 續航維度獎勵「前半偷懶後半補」—— 負分割一律 100。8/13 摘要自己寫「平均達標是後段補回來的」，分數卻給滿分 20/20。 |
| 4 | P1 | 「撐得住／撐不住」只有 2.05W 餘裕，±2 分鐘的對齊位移就會翻掉（實測 -120s→沒撐住、+60s→撐得住）。這天因為按了手動 lap 才可信，忘了按就是擲骰子。後果是「四週表全部 -10W」。 |
| 5 | P1 | 校準產出②兩個分支用**不同統計量**：做滿 20 分取「最佳 1200 秒滾動平均」（最大值），沒做滿取「整段平均外推」。9/10 驗收會拿滾動最大值跟今天的外推平均比，**偏向看起來有進步**。（附帶：全趟真實最佳 1200 秒是 199.4W，落在主測 1 尾段，不是主測 2。）|
| 6 | P2 | 對齊是全域最佳化，一段處方寫錯會把另一段正確的挪走（實測主測 2 寫錯 → 主測 1 錨點晚 18 分、分數從 100 掉到 86）。要寫進 `plan.json` 的 `_howToAdd` 警告。 |
| 7 | P2 | 處方的「±區間」是 plan.json 自己發明的（課表原文只寫單一瓦數），區間開多寬直接決定執行度分數。組間休息 2 分也是假設。 |
| 8 | P3 | 執行度只罰時間不足、不罰超過；超標地板 70 分（減量日踩太用力最低也只掉到 70，方向反了）；迴轉把滑行秒數算進分母不算分子；徽章用 `color-mix` 需 Chrome 111+。 |

**還沒上線**：`commit 24e90e9` 只推了程式，**沒有重生報告**。
`rides/index.json` 還沒有 score 欄位、`rides/2026-08-13.html` 的 ride-meta 還是舊的。
要看到徽章與對帳卡得跑：
```
SSL_CERT_FILE=/etc/ssl/cert.pem python3 scripts/build-ride-reports.py --only 2026-08-13 --overwrite
```

---

## 三、三條新 ITT 路段

> **2026-08-13 深夜補記：** 新增 `scripts/backfill-itt-efforts.py`，用自建偵測器掃
> `data/fit/*.fit` 把 Strava 漏配對的成績補進 `itt-segments.json`（合併不覆寫，
> 且 Strava 事後補配對到時會把多餘的 FIT 筆清掉）。已接進 `fit-sync.yml`。
> 目前 90 天的本機 FIT 只補到 2 筆：關渡→美堤 25:07（8/13）、河濱10K 22:15（7/22）。
> **要一年份得先跑 backfill=365 把 FIT 抓下來。**
> `fit-sync.yml` 的存檔步驟順序是刻意的（先 commit 只有自己會動的檔 → pull --rebase
> → 才在最新版上跑回補），為的是避開跟 strava-sync 搶同一個 JSON。


`31161895 關渡→美堤` / `4063039 河濱10K 木柵–永福` / `41498683 黃港河執行`。
配色改成有意義的分家：**河濱平路走藍色家族、山路暖色**。

掃描結果（本機 `FETCH_ALL=1 SCAN_SEGMENTS=1`，320 筆全史）：
- 黃港河執行：1 筆（2026-08-13，29:48）
- 關渡→美堤、河濱10K：**全史 0 筆**，Strava 從來沒配對過
- 既有六條 efforts 完全未變（12/34/10/29/4/3）→ 重掃沒破壞舊資料

**踩到的坑**：掃描尾聲撞 Strava 讀取額度，三條的 metadata／軌跡／地形都沒抓到。
以後加路段**不要跑全史掃描**，直接跑 `scripts/fetch-segment-streams.js` 與
`scripts/fetch-segment-terrain.js`，只抓缺的、約 3-6 次 API。

### 一個重要發現

**自建偵測器抓到一筆 Strava 漏掉的成績。** 同一趟（8/13）：

| | 自建 | Strava |
|---|---|---|
| 黃港河執行 | 1788.6 s | 1788 s（差 0.6 秒）|
| 關渡→美堤 | 1506.9 s | **0 筆** |

一條從沒調校過的新路段差不到一秒 → 偵測器可信。所以那筆 25:07 是 Strava 沒配對到。
**意義：去 Strava 化之後不只不會失去資料，還會拿到 Strava 沒給的。**

---

## 四、待辦（依價值排序）

> **2026-08-13 深夜補記 —— 以下已完成，commit `848d02e` / `5d16c86`：**
> 第二節表格的 **1～8 全部處理掉**（兩個 P0 + 續航負分割 + 撐得住餘裕警告 +
> 校準②統計量一致 + plan.json 兩條 `_howToAdd` 警告 + 迴轉分母排除滑行）。
> 8/13 報告已重生：總分 85.5 → **81.7（B+）**，因為續航從灌水的 100 掉到 79.5。
> 執行度 95.0、校準② 208W、規則違規 16 次/271 秒都沒變。
> 回歸矩陣：3 個 FIT × 9 個處方日 = 27 組合不炸，正確配對仍最高。
> 盤點文件第 1、2 項（wellness、活動計算欄位）的**擷取**也接上了，但沒接畫面。
>
> **只剩兩件需要你動手：**
> 1. 去 Actions 手動跑 **FIT Sync**，`backfill` 填 **365** —— 本機只有 90 天的 FIT，
>    一年份的 ITT 歷史要先有檔案才掃得到（`INTERVALS_API_KEY` 本機沒有）。
>    跑完 `git pull`，ITT 回補會在同一班自動做完。
> 2. 第一班跑完看一眼 `data/fit/_wellness.json` —— wellness 是用模擬回應離線測的，
>    真實 schema 沒驗過。

1. ~~修評分的兩個 P0~~ ✅ 已完成
2. **ITT 自動命名 —— 提案已完成待你核可**，見 `docs/ride-title-proposal.txt`（334 行）
   與原型 `tools/tcx/titles.py`（276 行，**尚未接進管線，接了才會生效**）。

   **兩個黃金樣本逐字重現**（含分隔符是半形空格 U+0020），複驗者親自重跑 20/20 一字不差。
   你口述的三個模板也被獨立算出來：劍中劍（05-19/26/27、06-16）、自風劍（05-21）、劍風劍（06-23）。

   三個關鍵發現：
   - **劍南路「去程」不是偵測不到，是反向。** 把「劍南路回程」的折線倒過來重建閘門，
     去程立刻現形。不需要新的半徑判斷，既有偵測器跑反向就好。
   - **折返收斂規則**：一筆通過若方向與前一筆保留的同名元素相反，視為同一趟來回的回程，
     不另計。08-11 中社路實際通過 9 次（4 正 5 反），標題只有 4 個「中」就是這條。
   - **「自」＝自強隧道，用 GPS 斷訊指紋定出來的**：找「斷訊 ≥30 s 且位移 ≥250 m」，
     05-21 命中 106 秒 / 821 m，隧道內高度 20-25 m 穿過雞南山（走地面要翻 150 m）。

   **複驗打穿的三點，接線前要處理**：
   - 2026-06-18 是**確認的假陰性**：那趟把「河濱10K」9963 m 從 0% 騎到 98%，
     只因終點錨點差 37 m（閘門半寬 30 m，差 7 m）沒偵測到。正確標題是「社 河」不是「社」。
     → 閘門半寬要能依路段調整，或對長路段放寬。
   - 「零誤報零漏報、誤差最大 2 秒」不精確，實測 **3 筆漏、2 筆多、最大 2.4 s**。
   - 自強隧道的門檻是調出來的：放寬到 ≥15 s / ≥120 m 就有 6 趟在同一定點
     (25.0852,121.5866) 重複斷訊 → 那個定點要先確認是什麼，不然會誤判成「自」。

   河濱三條的簡稱**還沒定案**（提案表暫用「河」「黃」），要你拍板。
3. **活動列表改吃 `rides/index.json`（FIT 產的）而不是 `data/strava.json`。**
   用戶要的方向。先確認哪些欄位只有 Strava 有（KOM／排名等全站數據拿不回來）。
4. **前端改用自建 ITT 成績**，不必等 Strava 配對（承第三節的發現）。
5. **ITT 檯面高度**：檯面只有 760px 寬（章節固定閱讀欄寬），只排得下 2 欄，
   9 張卡會變 5 列近 1700px。要 3 欄得把卡片從 272px 縮到 232px，會動到卡片內部設計。
6. `DESIGN.md` 比 `.impeccable/design.json` 新，等改動穩定後跑 `/impeccable document`。

---

## 五、環境備忘

- 本機 python 走 https 一律前綴 `SSL_CERT_FILE=/etc/ssl/cert.pem`（macOS framework build 沒有 CA bundle）
- `scripts/.env` 有 Strava 四把 key（gitignore），本機可直接跑 `fetch-strava.js`
- `INTERVALS_API_KEY` 在 GitHub secret，本機沒有
- 這台沒有 `gh` CLI；查 Actions 狀態用 `curl https://api.github.com/repos/chenhan20/linkTree/actions/...`
- headless 驗證的 CDP helper 在 scratchpad `cdp.mjs`；**探針檔第一行要 `return`**（helper 會包成 async IIFE）
- 設計 hook 對 `activity-modal.css` 報的 `bounce-easing`(L62) 與 `codex-grid-background`(L305)
  都是**既有程式碼、這次沒碰**，且後者是路線圖底（hook 規則自己說地圖可用）→ 判為誤報，未壓制
