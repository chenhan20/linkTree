# DESIGN.chronicle.md — 只管 `strava_chronicle.html`（運動編年史）

又一個獨立的視覺世界。根目錄的 `DESIGN.md` 只管 `strava.html`，對這一頁不適用；
impeccable hook 報的 `design-system-font` / `design-system-color` 在這裡是跨世界的預期 drift，不是缺陷。

## 概念

一支約 100 秒的 16-bit 橫向捲軸動畫，一個月一關，從 2025-04-27 第一趟騎到「今天」。
主角是他自己（橘色車衣，2025-08-12 之前騎藍色舊車，之後是黑色深框輪新車）。

- **路的高低是那個月騎了幾小時**：每個月一個平台，月與月之間一段坡道。比上個月拚＝上坡（站起來抽），偷懶＝下坡（腳翹起來滑）。
- **小山是真的爬升**：爬升 300 m 以上的騎乘，在它的日期隆起一座山（高度 ∝ 爬升），中社路那種日子就是山。
- **金幣是 Strava 路段 PR**：一趟 PR 越多撒越多顆（√PR，最多 8 顆），HUD 的金幣數是累積的 PR 總數。
- **季節跟著日期連續內插**：六個關鍵格（1 月冬雨、2 月底櫻花、4 月油桐、7 月大太陽、10 月芒草、11 月底楓紅），台北的季節不是溫帶的。
  下雨的日子只從活動名稱讀（「落湯雞」「下雨回頭」「結果下雨」），其餘是依月份氣候的隨機陣雨。
- **最後兩段**：中社路歷來每一次成績同場比賽（PR 剛好在第 4 小節強拍衝線），然後是夜景結算。

## 時間軸（整支片的骨架）

- 150 BPM，一小節 1.6 秒。開場 4 小節、每個月 2 小節（`EXTRA_BARS` 加長事件多的月份，最後一個月 +1 停在「今天」）、中社路 8、結算 6。
- **等速捲動**：一小節 = 世界裡 180 px。事件多的日子是「日曆變慢」而不是鏡頭變慢——每個事件有 `dwell` 權重，
  同一份累積權重同時決定時間與空間，所以三鐵、北彰這種整段的場景自己就有寬度（`span`）。
- 畫面是時間 T 的純函數（粒子也是 hash 算的），`__chronicle.render(t)` 可以直接跳到任何一格。

## 版面

320×180 內部解析度，畫面以整數裝置像素放大（`image-rendering: pixelated`）。

| 區域 | 位置 |
|---|---|
| HUD | 上緣 0–15：年月、季節字（彩色字，不加底色塊）、公里、爬升、金幣、eFTP（三週前比，漲綠三角、跌紅三角） |
| 字幕框 | 左上 (5,17) 高 46：標題（白）、引言（金，引號裡是他自己取的活動名稱）、當月數字（灰） |
| 彈出字 | 車手右前方、字幕框下緣以下，最多四格；滿了次要的直接不出、重要的把最快結束那格收掉——**一定要準時** |
| 大字（LEVEL UP） | 右上天空，右對齊 |
| 月時數長條 | 下緣 151–180：每月一根，超過 15.3 h 損益線是橘色，當月那根跟著日期長高 |

## 色彩

UI 的顏色只給真的分輕重的東西（見 `ai-look-tells`）：

| 值 | 用途 |
|---|---|
| `#ecead8` INK | 主文字 |
| `#a3a7b8` DIM | 次要數字 |
| `#f3c969` GOLD | PR／紀錄（彈出字、PR 影子、金幣）——只有這個用途 |
| `#ff9a5a` ORANGE | 他自己（車衣、「最近一次」）與「過線」的月份 |
| `#8fe0bf` / `#ffa39b` | eFTP 漲／跌的小三角，只跟數字一起出現 |
| `#15161b` / `#101218` | 畫布上像素字的描邊（壓在場景上的字一律描一圈，不用陰影） |
| `#9a7a2e` | 頁面上金色按鈕的實心下緣（按下去會往下沉 3px） |

場景色全部在 `SEASONS` 六格與 `MOD_NIGHT / MOD_RAIN / MOD_SUNSET` 三個修飾裡，不要在畫圖函式裡寫死季節色。

## 字

- 只用 **俐方體 11 號（Cubic 11，OFL）**，jsDelivr 釘 `@1.500`。**原生格是 12px**（11、13 會變形），大字用 24px（2×）。
- 畫布上的字先畫到離屏畫布再把 alpha 二值化（門檻 118），保證一顆字就是一格格的像素；整串快取在 `TXT`。
- 頁面外框（按鈕、時間、刻度）也只用 12／24px；下方說明文字用系統黑體。
- 3×5 迷你數字（`MICRO`）只給長條圖刻度用。

## 音樂

沒有音檔。Web Audio 即時合成：兩個方波（12.5%／25%／50% 占空比，`PeriodicWave`）、三角波貝斯、雜訊鼓，跟紅白機同一套樂器。

- 曲風跟季節走：春 C 大調、夏 D 大調（16 分音符的 hi-hat）、秋是王道進行 F–G–Em–Am、冬 A 小調半拍鼓＋回聲、
  只騎一次的十一月是空蕩的 Am→E、中社路 E 小調四拍大鼓、結算 C 大調，最後停在 G7（未完待續）。
- **鼓的密度跟著那個月騎了幾小時**：騎得少的月份連音樂都比較空。
- 整份樂譜在開始前排好（`buildScore`），播放時每格往前排 0.3 秒；拖時間軸就全部停掉、從新位置重排。
- 除錯：`__chronicle.probeAudio(t0, dur)` 用 OfflineAudioContext 把那一段算出來，回傳每 0.5 秒的峰值／RMS（量過：RMS 約 0.1、峰值 < 1）。

## 資料

`data/chronicle.json`，由 `scripts/build-chronicle.py` 產生（CI 的 fit-sync 每班跑，排在 ITT 回補之後）。
月時數用 `ride_hours.monthly_series()`（跟損益線同口徑）；逐筆活動是 Strava 封存＋封存之後的 intervals；
名稱裡有家庭字眼的活動只留數字、名稱清掉（這個 repo 是公開的），Walk／Hike 不收。

文案（每月一句標題＋引言）寫在頁面的 `STORY`；日期上的小插曲在 `CURATED`。新月份沒有文案時走 `STORY_DEFAULT`（時數＋PR 數）。

## 驗證

只走 headless CDP（Browser 窗格是隱藏分頁，rAF 不跑）。用 `__chronicle.render(t)` 跳格截圖，
把整片每個月的中段拼成一張總覽圖最快找到版面問題。
